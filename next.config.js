/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'links.papareact',
      'image.tmbd.org',
    ],
  },
}

module.exports = nextConfig

